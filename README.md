# image_recognize_system-cats-and-dogs-
 This project implements an image recognition system using Convolutional Neural Networks (CNNs). It demonstrates classification on popular benchmark datasets — MNIST (handwritten digits) and CIFAR-10 (colored objects) — using TensorFlow/Keras. Features Classifies images into 10 categories (digits for MNIST, objects for CIFAR-10).

Built with a simple CNN architecture.

Uses data preprocessing & normalization for better accuracy.

Includes training visualization (accuracy/loss curves).

Achieves high accuracy on MNIST (~98%) and decent performance on CIFAR-10.

🛠️ Tech Stack Language: Python

Framework: TensorFlow / Keras

Libraries: NumPy, Matplotlib

Environment: Google Colab

📂 Dataset MNIST Dataset – 70,000 grayscale handwritten digit images (0–9).

Loaded via: tf.keras.datasets.mnist

CIFAR-10 Dataset – 60,000 32×32 colored images in 10 classes.

Loaded via: tf.keras.datasets.cifar10
